<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard - Rabuste Coffee</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --gold: #d6a45a;
            --cream: #f5f2ee;
            --muted: #ccc;
        }
        
        body {
            font-family: 'Montserrat', sans-serif;
            background: #000000;
            margin: 0;
            padding: 0;
            overflow-x: hidden;
            color: #f5f2ee;
        }

        .sidebar {
            position: relative !important; /* Changed from fixed to relative */
            top: 0;
            bottom: 0;
            left: 0;
            z-index: 100;
            padding: 48px 0 0;
            box-shadow: inset -1px 0 0 rgba(0, 0, 0, .1);
            background-color: #000000;
            border-right: 1px solid #d6a45a;
            min-height: calc(100vh - 200px);
        }

        .sidebar .nav-link {
            font-weight: 500;
            color: #f5f2ee;
            padding: 0.75rem 1rem;
            border-radius: 0.25rem;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .sidebar .nav-link::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(214, 164, 90, 0.3), transparent);
            transition: left 0.5s ease;
        }

        .sidebar .nav-link:hover::before {
            left: 100%;
        }

        .sidebar .nav-link:hover {
            color: #d6a45a;
            background-color: rgba(214, 164, 90, 0.1);
            transform: translateX(5px);
        }

        .sidebar .nav-link.active {
            color: #d6a45a;
            background-color: rgba(214, 164, 90, 0.2);
            border-left: 3px solid #d6a45a;
        }

        .sidebar .nav-link:active {
            transform: scale(0.98);
        }

        /* Dashboard title animation */
        .dashboard-title {
            position: relative;
            animation: titleGlow 3s ease-in-out infinite alternate;
        }

        @keyframes titleGlow {
            0% {
                text-shadow: 0 0 10px rgba(214, 164, 90, 0.3);
                transform: translateY(0px);
            }
            50% {
                text-shadow: 0 0 20px rgba(214, 164, 90, 0.6);
                transform: translateY(-2px);
            }
            100% {
                text-shadow: 0 0 15px rgba(214, 164, 90, 0.4);
                transform: translateY(0px);
            }
        }

        main {
            margin-left: 0; /* Removed fixed margin since sidebar is now relative */
            padding-top: 80px;
            background-color: #0a0a0a;
            color: #f5f2ee;
            position: relative;
            min-height: 100vh;
        }

        main::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 20% 20%, rgba(214, 164, 90, 0.03) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(214, 164, 90, 0.03) 0%, transparent 50%),
                radial-gradient(circle at 40% 60%, rgba(214, 164, 90, 0.02) 0%, transparent 50%);
            pointer-events: none;
            z-index: 0;
        }

        .tab-content {
            position: relative;
            z-index: 1;
        }

        .tab-pane {
            position: relative;
            padding: 20px;
            border-radius: 12px;
            background: rgba(10, 10, 10, 0.8);
            backdrop-filter: blur(5px);
            border: 1px solid rgba(214, 164, 90, 0.3);
            margin-bottom: 20px;
        }

        .card {
            transition: transform 0.2s;
            background-color: rgba(26, 26, 26, 0.9);
            border: 1px solid #d6a45a;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(214, 164, 90, 0.1);
            position: relative;
            overflow: hidden;
        }

        .card .card-title,
        .card .card-text,
        .card h5,
        .card p,
        .card h4 {
            color: #ffffff !important;
            font-weight: 500;
        }

        /* Fix all form inputs and selects */
        .form-control,
        .form-select,
        .form-label {
            background-color: #1a1a1a !important;
            border: 1px solid #d6a45a !important;
            color: #f5f2ee !important;
        }

        .form-control:focus,
        .form-select:focus {
            background-color: #1a1a1a !important;
            border-color: #d6a45a !important;
            color: #f5f2ee !important;
            box-shadow: 0 0 0 0.2rem rgba(214, 164, 90, 0.25) !important;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 2px;
            background: linear-gradient(90deg, transparent, #d6a45a, transparent);
            transition: left 0.8s ease;
        }

        .card:hover::before {
            left: 100%;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(214, 164, 90, 0.2);
            border-color: #d6a45a;
        }

        .request-status {
            font-size: 0.875rem;
            padding: 0.25rem 0.5rem;
            border-radius: 0.25rem;
        }

        .status-pending {
            background-color: rgba(214, 164, 90, 0.2);
            color: #d6a45a;
        }

        .status-approved {
            background-color: rgba(40, 167, 69, 0.2);
            color: var(--gold);
        }

        .status-rejected {
            background-color: rgba(220, 53, 69, 0.2);
            color: #dc3545;
        }

        /* Franchise status styles */
        .franchise-status {
            font-size: 0.875rem;
            padding: 0.25rem 0.5rem;
            border-radius: 0.25rem;
            font-weight: 500;
        }

        .franchise-status.status-pending {
            background-color: rgba(255, 193, 7, 0.2);
            color: #ffc107;
            border: 1px solid #ffc107;
        }

        .franchise-status.status-approved {
            background-color: rgba(40, 167, 69, 0.2);
            color:var(--gold);
            border: 1px solidvar(--gold);
        }

        .franchise-status.status-rejected {
            background-color: rgba(220, 53, 69, 0.2);
            color: #dc3545;
            border: 1px solid #dc3545;
        }

        .franchise-status.status-under-review {
            background-color: rgba(23, 162, 184, 0.2);
            color: #17a2b8;
            border: 1px solid #17a2b8;
        }

        @media (max-width: 767.98px) {
            .sidebar {
                position: relative;
                height: auto;
                padding: 0;
                background-color: #0a0a0a;
                border-right: 1px solid #d6a45a;
            }
            
            main {
                margin-left: 0;
                padding-top: 20px;
                background-color: #0a0a0a;
                color: #f5f2ee;
            }
        }
    </style>
</head>
<body>
<div class="container-fluid py-4">
  <div class="row">
    <!-- Sidebar -->
    <div class="col-md-3 col-lg-2 d-md-block sidebar collapse">
      <div class="position-sticky pt-3">
        <!-- User Profile Section -->
        <div class="text-center mb-4">
          <div class="mb-3">
            <i class="bi bi-person-circle" style="font-size: 3rem; color: #d6a45a;"></i>
          </div>
          <h5 style="color: #f5f2ee; font-weight: 600;"><%= currentUser.name %></h5>
          <p style="color: #ccc; font-size: 0.9rem; margin: 0;"><%= currentUser.email %></p>
          <span class="badge" style="background-color: #d6a45a; color: #000;">
            <%= currentUser.isOAuthUser ? 'Google User' : 'Registered User' %>
          </span>
        </div>
        
        <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <div></div>
        <h1 class="h2 dashboard-title" style="color: #d6a45a; font-weight: 700; font-family: 'Playfair Display', serif; text-shadow: 0 0 10px rgba(214, 164, 90, 0.3);">Dashboard</h1>
        <div class="btn-toolbar mb-2 mb-md-0">
        </div>
      </div>
        <ul class="nav flex-column">
          <li class="nav-item">
            <a class="nav-link active" href="#wishlist" data-bs-toggle="pill">
              <i class="bi bi-heart"></i> Wishlist
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#cart" data-bs-toggle="pill">
              <i class="bi bi-cart"></i> Cart
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#orders" data-bs-toggle="pill">
              <i class="bi bi-receipt"></i> My Orders
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#workshops" data-bs-toggle="pill">
              <i class="bi bi-brush"></i> Workshops
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#franchise" data-bs-toggle="pill">
              <i class="bi bi-shop"></i> Franchise
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#requests" data-bs-toggle="pill">
              <i class="bi bi-envelope"></i> Requests
            </a>
          </li>
        </ul>
      </div>
    </div>

    <!-- Main content -->
    <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
      <div class="tab-content">
        <!-- Wishlist Tab -->
        <div class="tab-pane fade show active" id="wishlist">
          <h3 style="color: #d6a45a; font-weight: bold; letter-spacing: 1px; text-shadow: 0 0 5px rgba(214, 164, 90, 0.2);">My Wishlist</h3>
          <div class="row" id="wishlistItems">
            <!-- Wishlist items will be dynamically loaded here -->
          </div>
        </div>

        <!-- Cart Tab -->
        <div class="tab-pane fade" id="cart">
          <h3 style="color: #d6a45a; font-weight: bold; letter-spacing: 1px; text-shadow: 0 0 5px rgba(214, 164, 90, 0.2);">My Cart</h3>
          <div class="row" id="cartItems">
            <!-- Cart items will be dynamically loaded here -->
          </div>
        </div>

        <!-- Orders Tab -->
        <div class="tab-pane fade" id="orders">
          <h3 style="color: #d6a45a; font-weight: bold; letter-spacing: 1px; text-shadow: 0 0 5px rgba(214, 164, 90, 0.2);">My Orders</h3>
          
          <!-- Orders Filter -->
          <div class="row mb-4">
            <div class="col-md-6">
              <select class="form-select" id="orderStatusFilter" style="background-color: #1a1a1a; border: 1px solid #d6a45a; color: #f5f2ee;">
                <option value="">All Orders</option>
                <option value="pending">Pending</option>
                <option value="completed">Completed</option>
                <option value="cancelled">Cancelled</option>
              </select>
            </div>
            <div class="col-md-6">
              <select class="form-select" id="orderTypeFilter" style="background-color: #1a1a1a; border: 1px solid #d6a45a; color: #f5f2ee;">
                <option value="">All Types</option>
                <option value="menu">Menu Orders</option>
                <option value="art">Art Orders</option>
              </select>
            </div>
          </div>

          <!-- Orders List -->
          <div id="ordersList">
            <!-- Orders will be dynamically loaded here -->
          </div>
          
          <!-- Empty State -->
          <div id="emptyOrdersState" class="text-center py-5" style="display: none;">
            <i class="bi bi-receipt" style="font-size: 4rem; color: #666;"></i>
            <h4 style="color: #666; margin-top: 1rem;">No Orders Yet</h4>
            <p style="color: #999;">Your order history will appear here once you make your first purchase.</p>
            <a href="/menu" class="btn" style="background-color: #d6a45a; border: none; color: #000; margin-top: 1rem;">
              <i class="bi bi-cup-hot me-2"></i>Browse Menu
            </a>
          </div>
        </div>

        <!-- Workshops Tab -->
        <div class="tab-pane fade" id="workshops">
          <h3 style="color: #d6a45a; font-weight: bold; letter-spacing: 1px; text-shadow: 0 0 5px rgba(214, 164, 90, 0.2);">Registered Workshops</h3>
          <div class="row" id="workshopItems">
            <!-- Workshop items will be dynamically loaded here -->
          </div>
        </div>

        <!-- Franchise Tab -->
        <div class="tab-pane fade" id="franchise">
          <h3 style="color: #d6a45a; font-weight: bold; letter-spacing: 1px; text-shadow: 0 0 5px rgba(214, 164, 90, 0.2);">My Franchise Applications</h3>
          
          <!-- Franchise Applications List -->
          <div id="franchiseApplications">
            <!-- Franchise applications will be dynamically loaded here -->
          </div>
          
          <!-- Empty State -->
          <div id="emptyFranchiseState" class="text-center py-5" style="display: none;">
            <i class="bi bi-shop" style="font-size: 4rem; color: #666;"></i>
            <h4 style="color: #666; margin-top: 1rem;">No Franchise Applications</h4>
            <p style="color: #999;">You haven't submitted any franchise applications yet.</p>
            <a href="/franchise" class="btn" style="background-color: #d6a45a; border: none; color: #000; margin-top: 1rem;">
              <i class="bi bi-shop me-2"></i>Apply for Franchise
            </a>
          </div>
        </div>

        <!-- Requests Tab -->
        <div class="tab-pane fade" id="requests">
          <h3 style="color: #d6a45a; font-weight: bold; letter-spacing: 1px; text-shadow: 0 0 5px rgba(214, 164, 90, 0.2);">My Requests</h3>
          <div class="row" id="requestItems">
            <!-- Request items will be dynamically loaded here -->
          </div>
        </div>
      </div>
    </main>
  </div>
</div>

<!-- New Request Modal -->
<div class="modal fade" id="newRequestModal" tabindex="-1" aria-labelledby="newRequestModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content" style="background-color: #0a0a0a; border: 1px solid #d6a45a; color: #f5f2ee;">
      <div class="modal-header" style="border-bottom: 1px solid #d6a45a;">
        <h5 class="modal-title" id="newRequestModalLabel" style="color: #d6a45a;">Submit New Request</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close" style="filter: invert(1);"></button>
      </div>
      <div class="modal-body">
        <form id="requestForm">
          <div class="mb-3">
            <label for="requestType" class="form-label" style="color: #f5f2ee;">Request Type</label>
            <select class="form-select" id="requestType" required style="background-color: #1a1a1a; border: 1px solid #d6a45a; color: #f5f2ee;">
              <option value="">Select request type...</option>
              <option value="sell-art">Sell Art</option>
              <option value="conduct-workshop">Conduct Workshop</option>
            </select>
          </div>
          
          <div class="mb-3">
            <label for="requestTitle" class="form-label" style="color: #f5f2ee;">Title</label>
            <input type="text" class="form-control" id="requestTitle" required style="background-color: #1a1a1a; border: 1px solid #d6a45a; color: #f5f2ee;">
          </div>
          <div class="mb-3">
            <label for="requestDescription" class="form-label" style="color: #f5f2ee;">Description</label>
            <textarea class="form-control" id="requestDescription" rows="4" required style="background-color: #1a1a1a; border: 1px solid #d6a45a; color: #f5f2ee;"></textarea>
          </div>
          <div class="mb-3" id="artDetails" style="display: none;">
            <label for="artPrice" class="form-label" style="color: #f5f2ee;">Asking Price ($)</label>
            <input type="number" class="form-control" id="artPrice" min="0" step="0.01" style="background-color: #1a1a1a; border: 1px solid #d6a45a; color: #f5f2ee;">
          </div>
          <div class="mb-3" id="workshopDetails" style="display: none;">
            <label for="workshopDate" class="form-label" style="color: #f5f2ee;">Preferred Date</label>
            <input type="date" class="form-control" id="workshopDate" style="background-color: #1a1a1a; border: 1px solid #d6a45a; color: #f5f2ee;">
          </div>
        </form>
      </div>
      <div class="modal-footer" style="border-top: 1px solid #d6a45a; background-color: #0a0a0a;">
        <button type="button" class="btn" data-bs-dismiss="modal" style="background-color: transparent; border: 1px solid #d6a45a; color: #d6a45a;">Cancel</button>
        <button type="button" class="btn" id="submitRequest" style="background-color: #d6a45a; border: none; color: #000;">Submit Request</button>
      </div>
    </div>
  </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<script>
// API functions for dynamic data
async function fetchWishlist() {
  try {
    const response = await fetch('/api/wishlist');
    return await response.json();
  } catch (error) {
    console.error('Error fetching wishlist:', error);
    return [];
  }
}

async function fetchCart() {
  try {
    const response = await fetch('/api/cart');
    return await response.json();
  } catch (error) {
    console.error('Error fetching cart:', error);
    return [];
  }
}

async function fetchOrders() {
  try {
    console.log('Fetching user orders...');
    const response = await fetch('/api/user/orders');
    if (!response.ok) {
      throw new Error('Failed to fetch orders');
    }
    const orders = await response.json();
    console.log('Fetched orders:', orders);
    return orders;
  } catch (error) {
    console.error('Error fetching orders:', error);
    return [];
  }
}

async function fetchWorkshops() {
  try {
    const response = await fetch('/api/workshops');
    return await response.json();
  } catch (error) {
    console.error('Error fetching workshops:', error);
    return [];
  }
}

async function fetchRequests() {
  try {
    const response = await fetch('/api/requests');
    return await response.json();
  } catch (error) {
    console.error('Error fetching requests:', error);
    return [];
  }
}

async function fetchFranchise() {
  try {
    console.log('Fetching user franchise applications...');
    const response = await fetch('/api/user/franchise');
    if (!response.ok) {
      throw new Error('Failed to fetch franchise applications');
    }
    const data = await response.json();
    console.log('Fetched franchise applications:', data);
    return data.success ? data.applications : [];
  } catch (error) {
    console.error('Error fetching franchise applications:', error);
    return [];
  }
}

function renderWishlist(items) {
  const container = document.getElementById('wishlistItems');
  
  if (items.length === 0) {
    container.innerHTML = `
      <div class="col-12 text-center py-5">
        <p style="color: #ffffff; font-size: 0.85rem; margin-bottom: 15px; font-weight: 500;">No items in your wishlist yet.</p>
        <a href="/menu" class="btn" style="background-color: #d6a45a; border: none; color: #000;">Browse Menu</a>
      </div>
    `;
  } else {
    container.innerHTML = items.map(item => `
      <div class="col-md-6 col-lg-4 mb-4">
        <div class="card">
          <div class="card-body">
            
            <h5 class="card-title">${item.name}</h5>
            <p class="card-text">Price: ₹${item.price}</p>
            <div class="d-flex gap-2">
              <button class="btn btn-sm" style="background-color: #d6a45a; border: none; color: #000;" onclick="moveToCart('${item.itemId}', '${item.name}', ${item.price})">Move to Cart</button>
              <button class="btn btn-sm btn-outline-danger ms-2" onclick="removeFromWishlist('${item.itemId}')">Remove</button>
            </div>
          </div>
        </div>
      </div>
    `).join('');
  }
}

function renderCart(items) {
  const container = document.getElementById('cartItems');
  
  if (items.length === 0) {
    container.innerHTML = `
      <div class="col-12 text-center py-5">
        <p style="color: #ffffff; font-size: 0.85rem; margin-weight: 500;">Your cart is empty.</p>
        <a href="/menu" class="btn" style="background-color: #d6a45a; border: none; color: #000;">View Menu</a>
      </div>
    `;
    return;
  }

  let total = 0;
  const itemsHtml = items.map(item => {
    total += item.price * item.quantity;
    
    // Check if this is an art item - multiple ways to detect
    const isArtItem = item.type === 'art' || 
                     (item.image && item.image.includes('/assets/gallery/')) ||
                     (item.name && (item.name.includes('by ') || item.name.includes('Wilderness') || item.name.includes('Mountain') || item.name.includes('Serenity')));
    
    return `
      <div class="col-12 mb-3">
        <div class="card">
          <div class="card-body">
            <div class="row align-items-center">
              <div class="col-md-2">
                ${item.image ? `<img src="${item.image}" class="img-fluid rounded" alt="${item.name}">` : `<div class="bg-secondary rounded" style="width: 100%; height: 80px;"></div>`}
              </div>
              <div class="col-md-4">
                <h5>${item.name}</h5>
                <p>₹${item.price} each</p>
                ${isArtItem ? '<small style="color: #d6a45a;"><i class="bi bi-palette"></i> Unique Art Piece</small>' : ''}
              </div>
              <div class="col-md-3">
                ${isArtItem ? `
                  <div class="text-center">
                    <span class="badge" style="background-color: #d6a45a; color: #000; font-size: 0.9rem; padding: 0.4rem 0.8rem;">
                      <i class="bi bi-palette"></i> Unique Art
                    </span>
                  </div>
                ` : `
                  <div class="input-group">
                    <button class="btn btn-outline-secondary" onclick="updateQuantity('${item.itemId}', -1)">-</button>
                    <input type="number" class="form-control text-center" value="${item.quantity}" min="1" readonly>
                    <button class="btn btn-outline-secondary" onclick="updateQuantity('${item.itemId}', 1)">+</button>
                  </div>
                `}
              </div>
              <div class="col-md-2">
                <h5>₹${(item.price * item.quantity).toFixed(2)}</h5>
              </div>
              <div class="col-md-1">
                <button class="btn btn-outline-danger" onclick="removeFromCart('${item.itemId}')">
                  <i class="bi bi-trash"></i>
                </button>
              </div>
            </div>
          </div>
        </div>
      </div>
    `;
  }).join('');

  container.innerHTML = itemsHtml + `
    <div class="col-12">
      <div class="card">
        <div class="card-body">
          <div class="row">
            <div class="col-md-8">
              <h4 style="color: #d6a45a; font-weight: bold;">Total: ₹${total.toFixed(2)}</h4>
            </div>
            <div class="col-md-4 text-end">
              <button class="btn" id="checkoutBtn" style="background-color: #d6a45a; border: none; color: #000;" onclick="proceedToCheckout()">Proceed to Checkout</button>
            </div>
          </div>
        </div>
      </div>
    </div>
  `;
}

function renderOrders(orders) {
  console.log('renderOrders called with:', orders);
  const container = document.getElementById('ordersList');
  const emptyState = document.getElementById('emptyOrdersState');
  
  if (!container) {
    console.error('ordersList container not found!');
    return;
  }
  
  if (!emptyState) {
    console.error('emptyOrdersState container not found!');
    return;
  }
  
  if (orders.length === 0) {
    console.log('No orders found, showing empty state');
    container.style.display = 'none';
    emptyState.style.display = 'block';
    return;
  }

  console.log('Rendering', orders.length, 'orders');
  container.style.display = 'block';
  emptyState.style.display = 'none';

  container.innerHTML = orders.map(order => {
    const statusColor = order.status === 'completed' ? 'success' : 
                       order.status === 'pending' ? 'warning' : 
                       order.status === 'cancelled' ? 'danger' : 'secondary';
    
    const orderDate = new Date(order.orderDate).toLocaleDateString('en-US', {
      year: 'numeric',
      month: 'short',
      day: 'numeric',
      hour: '2-digit',
      minute: '2-digit'
    });

    // Separate menu and art items
    const menuItems = order.items.filter(item => !item.type || item.type === 'menu');
    const artItems = order.items.filter(item => item.type === 'art');

    return `
      <div class="card mb-3" style="background-color: #1a1a1a; border: 1px solid #333;">
        <div class="card-header d-flex justify-content-between align-items-center" style="background-color: #2a2a2a; border-bottom: 1px solid #333;">
          <div>
            <h6 class="mb-0" style="color: #d6a45a;">Order #${order._id.slice(-6).toUpperCase()}</h6>
            <small style="color: #ccc;">${orderDate}</small>
          </div>
          <span class="badge bg-${statusColor}">${order.status.charAt(0).toUpperCase() + order.status.slice(1)}</span>
        </div>
        <div class="card-body" style="color: #f5f2ee;">
          <div class="row">
            <div class="col-md-8">
              ${menuItems.length > 0 ? `
                <h6 style="color: #d6a45a; margin-bottom: 10px;">
                  <i class="bi bi-cup-hot me-2"></i>Menu Items:
                </h6>
                <div class="row mb-3">
                  ${menuItems.map(item => `
                    <div class="col-md-6 mb-2">
                      <div class="d-flex align-items-center">
                        ${item.image ? `<img src="${item.image}" class="rounded me-2" style="width: 40px; height: 40px; object-fit: cover;" alt="${item.name}" onerror="this.src='/assets/placeholder.jpg'">` : `<div class="bg-secondary rounded me-2" style="width: 40px; height: 40px;"></div>`}
                        <div>
                          <small class="fw-bold">${item.name}</small><br>
                          <small style="color: #ccc;">Qty: ${item.quantity} × ₹${item.price}</small>
                        </div>
                      </div>
                    </div>
                  `).join('')}
                </div>
              ` : ''}
              
              ${artItems.length > 0 ? `
                <h6 style="color: #d6a45a; margin-bottom: 10px;">
                  <i class="bi bi-palette me-2"></i>Art Pieces:
                </h6>
                <div class="row">
                  ${artItems.map(item => `
                    <div class="col-md-6 mb-2">
                      <div class="d-flex align-items-center">
                        ${item.image ? `<img src="${item.image}" class="rounded me-2" style="width: 40px; height: 40px; object-fit: cover;" alt="${item.name}" onerror="this.src='/assets/placeholder.jpg'">` : `<div class="bg-secondary rounded me-2" style="width: 40px; height: 40px;"></div>`}
                        <div>
                          <small class="fw-bold">${item.name}</small><br>
                          <small style="color: #ccc;">Unique Art Piece - ₹${item.price}</small>
                        </div>
                      </div>
                    </div>
                  `).join('')}
                </div>
              ` : ''}
            </div>
            <div class="col-md-4 text-end">
              <h5 style="color: #d6a45a;">Total: ₹${order.totalAmount.toFixed(2)}</h5>
              ${order.status === 'pending' ? `
                <small style="color: #f39c12;">
                  <i class="bi bi-clock"></i> Order is being prepared
                </small>
              ` : order.status === 'completed' ? `
                <small style="color:var(--gold);">
                  <i class="bi bi-check-circle"></i> Order completed
                </small>
              ` : order.status === 'cancelled' ? `
                <small style="color: #dc3545;">
                  <i class="bi bi-x-circle"></i> Order cancelled
                </small>
              ` : ''}
            </div>
          </div>
        </div>
      </div>
    `;
  }).join('');
}

function renderWorkshops(registrations) {
  const container = document.getElementById('workshopItems');
  
  if (registrations.length === 0) {
    container.innerHTML = `
      <div class="col-12 text-center py-5">
        <p style="color: #ffffff; font-size: 0.85rem; margin-bottom: 15px; font-weight: 500;">Discover your creative potential</p>
        <a href="/workshops" class="btn" style="background-color: #d6a45a; border: none; color: #000;">Browse Workshops</a>
      </div>
    `;
    return;
  }

  container.innerHTML = registrations.map(registration => `
    <div class="col-md-6 mb-4">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">${registration.workshopName}</h5>
          <p class="card-text">
            <i class="bi bi-calendar"></i> ${registration.workshopDate}<br>
            <i class="bi bi-person"></i> ${registration.userName}<br>
            <i class="bi bi-envelope"></i> ${registration.userEmail}
          </p>
          <span class="badge bg-success">${registration.status}</span>
          <button class="btn btn-sm btn-outline-primary ms-2">View Details</button>
        </div>
      </div>
    </div>
  `).join('');
}

function renderRequests(requests) {
  const container = document.getElementById('requestItems');
  
  if (requests.length === 0) {
    container.innerHTML = `
      <div class="col-12 text-center py-5">
        <p style="color: #ffffff; font-size: 0.85rem; margin-bottom: 15px; font-weight: 500;">Start your creative journey with us</p>
        <button type="button" class="btn" style="background-color: #d6a45a; border: none; color: #000;" data-bs-toggle="modal" data-bs-target="#newRequestModal">
          Submit Request
        </button>
      </div>
    `;
    return;
  }

  container.innerHTML = requests.map(request => `
    <div class="col-12 mb-3">
      <div class="card">
        <div class="card-body">
          <div class="row align-items-center">
            <div class="col-md-8">
              <h5 class="card-title">${request.title}</h5>
              <p class="card-text">
                Type: ${request.type === 'sell-art' ? 'Sell Art' : 'Conduct Workshop'}<br>
                Submitted: ${request.submittedDate}
              </p>
            </div>
            <div class="col-md-4 text-end">
              <span class="request-status status-${request.status}">${request.status.toUpperCase()}</span>
              <button class="btn btn-sm btn-outline-primary ms-2">View Details</button>
            </div>
          </div>
        </div>
      </div>
    </div>
  `).join('');
}

function renderFranchise(applications) {
  console.log('renderFranchise called with:', applications);
  const container = document.getElementById('franchiseApplications');
  const emptyState = document.getElementById('emptyFranchiseState');
  
  if (!container) {
    console.error('franchiseApplications container not found!');
    return;
  }
  
  if (!emptyState) {
    console.error('emptyFranchiseState container not found!');
    return;
  }
  
  if (applications.length === 0) {
    console.log('No franchise applications found, showing empty state');
    container.style.display = 'none';
    emptyState.style.display = 'block';
    return;
  }

  console.log('Rendering', applications.length, 'franchise applications');
  container.style.display = 'block';
  emptyState.style.display = 'none';

  container.innerHTML = applications.map(app => {
    const statusColor = app.status === 'approved' ? 'success' : 
                       app.status === 'pending' ? 'warning' : 
                       app.status === 'under-review' ? 'info' :
                       app.status === 'rejected' ? 'danger' : 'secondary';
    
    const statusIcon = app.status === 'approved' ? 'check-circle' : 
                      app.status === 'pending' ? 'clock' : 
                      app.status === 'under-review' ? 'hourglass-split' :
                      app.status === 'rejected' ? 'x-circle' : 'question-circle';
    
    const submittedDate = new Date(app.submittedAt).toLocaleDateString('en-US', {
      year: 'numeric',
      month: 'short',
      day: 'numeric'
    });

    const reviewedDate = app.reviewedAt ? new Date(app.reviewedAt).toLocaleDateString('en-US', {
      year: 'numeric',
      month: 'short',
      day: 'numeric'
    }) : null;

    return `
      <div class="card mb-3" style="background-color: #1a1a1a; border: 1px solid #333;">
        <div class="card-header d-flex justify-content-between align-items-center" style="background-color: #2a2a2a; border-bottom: 1px solid #333;">
          <div>
            <h6 class="mb-0" style="color: #d6a45a;">
              <i class="bi bi-shop me-2"></i>Franchise Application
            </h6>
            <small style="color: #ccc;">Submitted: ${submittedDate}</small>
          </div>
          <span class="badge bg-${statusColor}">
            <i class="bi bi-${statusIcon} me-1"></i>
            ${app.status.charAt(0).toUpperCase() + app.status.slice(1).replace('-', ' ')}
          </span>
        </div>
        <div class="card-body" style="color: #f5f2ee;">
          <div class="row">
            <div class="col-md-8">
              <h6 style="color: #d6a45a; margin-bottom: 10px;">
                <i class="bi bi-person me-2"></i>Application Details:
              </h6>
              <div class="row mb-3">
                <div class="col-md-6">
                  <small class="fw-bold">Location:</small><br>
                  <small style="color: #ccc;">${app.city}</small>
                </div>
                <div class="col-md-6">
                  <small class="fw-bold">Investment Range:</small><br>
                  <small style="color: #ccc;">${app.investmentRange}</small>
                </div>
              </div>
              <div class="row mb-3">
                <div class="col-md-6">
                  <small class="fw-bold">Timeline:</small><br>
                  <small style="color: #ccc;">${app.expectedTimeline}</small>
                </div>
                <div class="col-md-6">
                  <small class="fw-bold">Phone:</small><br>
                  <small style="color: #ccc;">${app.phoneNumber}</small>
                </div>
              </div>
              ${app.businessExperience ? `
                <div class="mb-3">
                  <small class="fw-bold">Business Experience:</small><br>
                  <small style="color: #ccc;">${app.businessExperience}</small>
                </div>
              ` : ''}
            </div>
            <div class="col-md-4 text-end">
              <div class="mb-2">
                <small style="color: #ccc;">Application ID:</small><br>
                <code style="color: #d6a45a;">#${app._id.slice(-6).toUpperCase()}</code>
              </div>
              ${reviewedDate ? `
                <div class="mb-2">
                  <small style="color: #ccc;">Reviewed:</small><br>
                  <small style="color: #f5f2ee;">${reviewedDate}</small>
                  ${app.reviewedBy ? `<br><small style="color: #ccc;">by ${app.reviewedBy}</small>` : ''}
                </div>
              ` : ''}
              ${app.adminNotes ? `
                <div class="alert alert-info" style="background-color: rgba(23, 162, 184, 0.1); border: 1px solid #17a2b8; color: #17a2b8; font-size: 0.8rem; padding: 0.5rem;">
                  <strong>Admin Notes:</strong><br>
                  ${app.adminNotes}
                </div>
              ` : ''}
              ${app.status === 'pending' ? `
                <small style="color: #f39c12;">
                  <i class="bi bi-clock"></i> Under review by our team
                </small>
              ` : app.status === 'approved' ? `
                <small style="color:var(--gold);">
                  <i class="bi bi-check-circle"></i> Congratulations! Application approved
                </small>
              ` : app.status === 'rejected' ? `
                <small style="color: #dc3545;">
                  <i class="bi bi-x-circle"></i> Application not approved
                </small>
              ` : app.status === 'under-review' ? `
                <small style="color: #17a2b8;">
                  <i class="bi bi-hourglass-split"></i> Currently under detailed review
                </small>
              ` : ''}
            </div>
          </div>
        </div>
      </div>
    `;
  }).join('');
}

async function removeFromWishlist(itemId) {
  try {
    const response = await fetch(`/api/wishlist/${itemId}`, {
      method: 'DELETE'
    });
    const result = await response.json();
    if (result.success) {
      // Refresh wishlist
      const wishlist = await fetchWishlist();
      renderWishlist(wishlist);
    }
  } catch (error) {
    console.error('Error removing from wishlist:', error);
  }
}

async function removeFromCart(itemId) {
  try {
    const response = await fetch(`/api/cart/${itemId}`, {
      method: 'DELETE'
    });
    const result = await response.json();
    if (result.success) {
      // Refresh cart
      const cart = await fetchCart();
      renderCart(cart);
    }
  } catch (error) {
    console.error('Error removing from cart:', error);
  }
}

async function moveToCart(itemId, name, price) {
  try {
    // First add to cart
    const response = await fetch('/api/cart', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ itemId, name, price, quantity: 1 })
    });
    const result = await response.json();
    
    if (result.success) {
      // Then remove from wishlist
      await removeFromWishlist(itemId);
      // Refresh cart
      const cart = await fetchCart();
      renderCart(cart);
    } else {
      console.error('Failed to add to cart:', result.message);
    }
  } catch (error) {
    console.error('Error moving to cart:', error);
  }
}

async function updateQuantity(itemId, change) {
  try {
    console.log('Updating quantity for item:', itemId, 'change:', change);
    
    // Get current cart to find the item
    const cart = await fetchCart();
    const item = cart.find(item => item.itemId === itemId);
    
    if (!item) {
      console.error('Item not found in cart:', itemId);
      return;
    }
    
    // Check if this is an art item - art items cannot have quantity changed
    const isArtItem = item.type === 'art' || 
                     (item.image && item.image.includes('/assets/gallery/')) ||
                     (item.name && (item.name.includes('by ') || item.name.includes('Wilderness') || item.name.includes('Mountain') || item.name.includes('Serenity')));
    
    if (isArtItem) {
      showToast('Art pieces are unique items and cannot have quantity changed. You can only remove them from cart.', 'info');
      return;
    }
    
    const newQuantity = item.quantity + change;
    console.log('New quantity:', newQuantity);
    
    if (newQuantity <= 0) {
      // Remove item if quantity is 0 or less
      await removeFromCart(itemId);
    } else {
      // Update quantity
      const response = await fetch(`/api/cart/${itemId}`, {
        method: 'PATCH',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({ quantity: newQuantity })
      });
      
      const result = await response.json();
      console.log('Update result:', result);
      
      if (result.success) {
        // Refresh cart
        const updatedCart = await fetchCart();
        renderCart(updatedCart);
      } else {
        console.error('Failed to update quantity:', result.message);
      }
    }
  } catch (error) {
    console.error('Error updating quantity:', error);
  }
}

// Filter orders function
function filterOrders() {
  const statusFilter = document.getElementById('orderStatusFilter').value;
  const typeFilter = document.getElementById('orderTypeFilter').value;
  
  let filteredOrders = window.allOrders || [];
  
  // Filter by status
  if (statusFilter) {
    filteredOrders = filteredOrders.filter(order => order.status === statusFilter);
  }
  
  // Filter by type
  if (typeFilter === 'menu') {
    filteredOrders = filteredOrders.filter(order => 
      order.items.some(item => !item.type || item.type === 'menu')
    );
  } else if (typeFilter === 'art') {
    filteredOrders = filteredOrders.filter(order => 
      order.items.some(item => item.type === 'art')
    );
  }
  
  renderOrders(filteredOrders);
}

document.addEventListener('DOMContentLoaded', async function() {
  // Load dashboard data from API
  console.log('Loading dashboard data...');
  const wishlist = await fetchWishlist();
  const cart = await fetchCart();
  const orders = await fetchOrders();
  const workshops = await fetchWorkshops();
  const requests = await fetchRequests();
  const franchise = await fetchFranchise();

  console.log('Dashboard data loaded:', {
    wishlist: wishlist.length,
    cart: cart.length, 
    orders: orders.length,
    workshops: workshops.length,
    requests: requests.length,
    franchise: franchise.length
  });

  renderWishlist(wishlist);
  renderCart(cart);
  renderOrders(orders);
  renderWorkshops(workshops);
  renderRequests(requests);
  renderFranchise(franchise);

  // Auto-refresh wishlist every 5 seconds
  setInterval(async () => {
    try {
      const currentWishlist = await fetchWishlist();
      renderWishlist(currentWishlist);
    } catch (error) {
      console.error('Error refreshing wishlist:', error);
    }
  }, 5000);

  // Store orders globally for filtering
  window.allOrders = orders;

  // Add event listeners for order filters
  const statusFilter = document.getElementById('orderStatusFilter');
  const typeFilter = document.getElementById('orderTypeFilter');
  
  if (statusFilter) {
    statusFilter.addEventListener('change', filterOrders);
  }
  
  if (typeFilter) {
    typeFilter.addEventListener('change', filterOrders);
  }

  // Handle request type change
  const requestTypeSelect = document.getElementById('requestType');
  if (requestTypeSelect) {
    requestTypeSelect.addEventListener('change', function() {
      const artDetails = document.getElementById('artDetails');
      const workshopDetails = document.getElementById('workshopDetails');
      
      if (this.value === 'sell-art') {
        artDetails.style.display = 'block';
        workshopDetails.style.display = 'none';
      } else if (this.value === 'conduct-workshop') {
        artDetails.style.display = 'none';
        workshopDetails.style.display = 'block';
      } else {
        artDetails.style.display = 'none';
        workshopDetails.style.display = 'none';
      }
    });
  }

  // Handle form submission
  const form = document.getElementById('requestForm');
  if (form) {
    form.addEventListener('submit', async function(e) {
      e.preventDefault();
      
      const requestType = document.getElementById('requestType').value;
      const title = document.getElementById('requestTitle').value;
      const description = document.getElementById('requestDescription').value;
      
      if (!requestType || !title || !description) {
        alert('Please fill in all required fields');
        return;
      }
      
      const request = {
        type: requestType,
        title: title,
        description: description,
        status: 'pending',
        submittedDate: new Date().toISOString().split('T')[0]
      };
      
      if (requestType === 'sell-art') {
        request.price = document.getElementById('artPrice').value;
      } else if (requestType === 'conduct-workshop') {
        request.preferredDate = document.getElementById('workshopDate').value;
      }

      const result = await submitRequest(request);
      if (result.success) {
        alert('Request submitted successfully!');
        form.reset();
        const modal = bootstrap.Modal.getInstance(document.getElementById('newRequestModal'));
        modal.hide();
        
        // Refresh requests list
        const updatedRequests = await fetchRequests();
        renderRequests(updatedRequests);
      } else {
        alert('Error submitting request. Please try again.');
      }
    });
  }

  // Handle submit button
  const submitButton = document.getElementById('submitRequest');
  if (submitButton) {
    submitButton.addEventListener('click', async function() {
      const requestType = document.getElementById('requestType').value;
      const title = document.getElementById('requestTitle').value;
      const description = document.getElementById('requestDescription').value;
      
      if (!requestType || !title || !description) {
        alert('Please fill in all required fields');
        return;
      }
      
      const request = {
        type: requestType,
        title: title,
        description: description,
        status: 'pending',
        submittedDate: new Date().toISOString().split('T')[0]
      };
      
      if (requestType === 'sell-art') {
        request.price = document.getElementById('artPrice').value;
      } else if (requestType === 'conduct-workshop') {
        request.preferredDate = document.getElementById('workshopDate').value;
      }

      const result = await submitRequest(request);
      if (result.success) {
        alert('Request submitted successfully!');
        document.getElementById('requestForm').reset();
        const modal = bootstrap.Modal.getInstance(document.getElementById('newRequestModal'));
        modal.hide();
        
        // Refresh requests list
        const updatedRequests = await fetchRequests();
        renderRequests(updatedRequests);
      } else {
        alert('Error submitting request. Please try again.');
      }
    });
  }
});

async function submitRequest(request) {
  try {
    const response = await fetch('/api/requests', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(request)
    });
    return await response.json();
  } catch (error) {
    console.error('Error submitting request:', error);
    return { success: false };
  }
}

// Checkout functionality
async function proceedToCheckout() {
  try {
    // Check if cart is empty
    const cart = await fetchCart();
    if (!cart || cart.length === 0) {
      alert('Your cart is empty. Please add items before checkout.');
      return;
    }
    
    const checkoutBtn = document.getElementById('checkoutBtn');
    const originalText = checkoutBtn.innerHTML;
    
    // Show loading state
    checkoutBtn.disabled = true;
    checkoutBtn.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span>Processing...';
    
    const response = await fetch('/api/checkout', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      }
    });
    
    const result = await response.json();
    
    if (result.success) {
      // Show success message
      showCheckoutSuccess(result.orderId, result.totalAmount);
      
      // Refresh cart display
      const updatedCart = await fetchCart();
      renderCart(updatedCart);
      
      // Refresh orders display
      const updatedOrders = await fetchOrders();
      renderOrders(updatedOrders);
      window.allOrders = updatedOrders;
      
    } else {
      throw new Error(result.message || 'Checkout failed');
    }
    
  } catch (error) {
    console.error('Checkout error:', error);
    
    let errorMessage = 'Failed to place order. Please try again.';
    if (error.message.includes('Please login')) {
      errorMessage = 'Please login to place an order.';
    } else if (error.message.includes('Cart is empty')) {
      errorMessage = 'Your cart is empty. Please add items before checkout.';
    }
    
    alert(errorMessage);
  } finally {
    // Reset button
    const checkoutBtn = document.getElementById('checkoutBtn');
    if (checkoutBtn) {
      checkoutBtn.disabled = false;
      checkoutBtn.innerHTML = 'Proceed to Checkout';
    }
  }
}

function showCheckoutSuccess(orderId, totalAmount) {
  // Create success modal
  const successModal = document.createElement('div');
  successModal.className = 'modal fade';
  successModal.id = 'checkoutSuccessModal';
  successModal.innerHTML = `
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content" style="background-color: #1a1a1a; border: 1px solid #d6a45a;">
        <div class="modal-header" style="border-bottom: 1px solid #d6a45a;">
          <h5 class="modal-title" style="color: #d6a45a;">
            <i class="bi bi-check-circle-fill me-2"></i>Order Placed Successfully!
          </h5>
        </div>
        <div class="modal-body text-center" style="color: #f5f2ee;">
          <div class="mb-3">
            <i class="bi bi-check-circle" style="font-size: 3rem; color:var(--gold);"></i>
          </div>
          <h4 style="color: #d6a45a;">Thank You!</h4>
          <p>Your order has been placed successfully and is now being processed.</p>
          <div class="alert alert-success" style="background-color: rgba(40, 167, 69, 0.1); border: 1px solidvar(--gold); color:var(--gold);">
            <strong>Order ID:</strong> #${orderId.toString().slice(-6).toUpperCase()}<br>
            <strong>Total Amount:</strong> ₹${totalAmount.toFixed(2)}
          </div>
          <p class="small">You can track your order status in the "My Orders" section. Your order will be prepared shortly!</p>
        </div>
        <div class="modal-footer" style="border-top: 1px solid #d6a45a;">
          <button type="button" class="btn" style="background-color: #d6a45a; border: none; color: #000;" data-bs-dismiss="modal">Continue Shopping</button>
          <a href="/menu" class="btn btn-outline-light">Browse Menu</a>
        </div>
      </div>
    </div>
  `;
  
  document.body.appendChild(successModal);
  
  // Show modal
  const modal = new bootstrap.Modal(successModal);
  modal.show();
  
  // Remove modal from DOM when hidden
  successModal.addEventListener('hidden.bs.modal', function() {
    document.body.removeChild(successModal);
  });
  
  // Also show a toast notification
  showToast('Order placed successfully!', 'success');
}

// Toast notification system
function showToast(message, type = 'info') {
  // Create toast container if it doesn't exist
  let toastContainer = document.getElementById('toastContainer');
  if (!toastContainer) {
    toastContainer = document.createElement('div');
    toastContainer.id = 'toastContainer';
    toastContainer.className = 'toast-container position-fixed top-0 end-0 p-3';
    toastContainer.style.zIndex = '9999';
    document.body.appendChild(toastContainer);
  }
  
  // Create toast
  const toast = document.createElement('div');
  toast.className = 'toast';
  toast.setAttribute('role', 'alert');
  toast.innerHTML = `
    <div class="toast-header" style="background-color: #1a1a1a; border-bottom: 1px solid #d6a45a;">
      <i class="bi bi-${type === 'success' ? 'check-circle-fill text-success' : 'info-circle-fill text-info'} me-2"></i>
      <strong class="me-auto" style="color: #d6a45a;">Notification</strong>
      <button type="button" class="btn-close" data-bs-dismiss="toast" style="filter: invert(1);"></button>
    </div>
    <div class="toast-body" style="background-color: #1a1a1a; color: #f5f2ee;">
      ${message}
    </div>
  `;
  
  toastContainer.appendChild(toast);
  
  // Show toast
  const bsToast = new bootstrap.Toast(toast, { delay: 4000 });
  bsToast.show();
  
  // Remove toast from DOM after it's hidden
  toast.addEventListener('hidden.bs.toast', function() {
    toastContainer.removeChild(toast);
  });
}
</script>

</body>
</html>