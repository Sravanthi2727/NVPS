<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submit Your Art - Rabuste Coffee</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --gold: #d6a45a;
            --gold-soft: #e3b873;
            --text: #f5f2ee;
            --muted: rgba(245, 242, 238, 0.7);
        }

        body {
            font-family: 'Inter', sans-serif;
            background: #000;
            color: var(--text);
            margin: 0;
            padding: 0;
        }

        .art-request-container {
            min-height: 100vh;
            padding: 2rem 0;
            background: linear-gradient(135deg, #000 0%, #1a1a1a 100%);
        }

        .art-request-header {
            text-align: center;
            margin-bottom: 3rem;
            padding: 2rem 0;
        }

        .art-request-header h1 {
            color: var(--gold);
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 1rem;
        }

        .art-request-header p {
            color: var(--muted);
            font-size: 1.2rem;
            max-width: 600px;
            margin: 0 auto;
        }

        .art-form-card {
            background: rgba(26, 26, 26, 0.9);
            border-radius: 20px;
            padding: 3rem;
            box-shadow: 0 20px 40px rgba(214, 164, 90, 0.1);
            border: 2px solid rgba(214, 164, 90, 0.2);
            backdrop-filter: blur(10px);
        }

        .form-section {
            margin-bottom: 2.5rem;
        }

        .form-section h3 {
            color: var(--gold);
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
            padding-bottom: 0.5rem;
            border-bottom: 2px solid rgba(214, 164, 90, 0.3);
        }

        .form-label {
            color: var(--text);
            font-weight: 500;
            margin-bottom: 0.75rem;
            font-size: 1rem;
        }

        .form-control, .form-select {
            background: rgba(0, 0, 0, 0.5);
            border: 2px solid rgba(214, 164, 90, 0.3);
            border-radius: 12px;
            color: var(--text);
            padding: 1rem;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .form-control:focus, .form-select:focus {
            background: rgba(0, 0, 0, 0.7);
            border-color: var(--gold);
            box-shadow: 0 0 0 0.2rem rgba(214, 164, 90, 0.25);
            color: var(--text);
        }

        .form-control::placeholder {
            color: var(--muted);
        }

        .file-upload-area {
            border: 3px dashed rgba(214, 164, 90, 0.5);
            border-radius: 16px;
            padding: 3rem 2rem;
            text-align: center;
            background: rgba(0, 0, 0, 0.3);
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .file-upload-area:hover {
            border-color: var(--gold);
            background: rgba(214, 164, 90, 0.05);
        }

        .file-upload-area.dragover {
            border-color: var(--gold);
            background: rgba(214, 164, 90, 0.1);
        }

        .file-upload-icon {
            font-size: 3rem;
            color: var(--gold);
            margin-bottom: 1rem;
        }

        .file-upload-text {
            color: var(--text);
            font-size: 1.1rem;
            margin-bottom: 0.5rem;
        }

        .file-upload-subtext {
            color: var(--muted);
            font-size: 0.9rem;
        }

        .file-preview {
            margin-top: 1rem;
            padding: 1rem;
            background: rgba(214, 164, 90, 0.1);
            border-radius: 12px;
            border: 1px solid rgba(214, 164, 90, 0.3);
        }

        .file-preview img {
            max-width: 200px;
            max-height: 200px;
            border-radius: 8px;
            object-fit: cover;
        }

        .btn-submit {
            background: linear-gradient(135deg, var(--gold), var(--gold-soft));
            border: none;
            color: #000;
            font-weight: 600;
            padding: 1rem 3rem;
            border-radius: 12px;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            width: 100%;
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(214, 164, 90, 0.4);
            color: #000;
        }

        .btn-back {
            background: transparent;
            border: 2px solid rgba(214, 164, 90, 0.5);
            color: var(--gold);
            padding: 0.75rem 2rem;
            border-radius: 12px;
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-block;
            margin-bottom: 2rem;
        }

        .btn-back:hover {
            background: rgba(214, 164, 90, 0.1);
            color: var(--gold);
            text-decoration: none;
        }

        .guidelines {
            background: rgba(214, 164, 90, 0.05);
            border: 1px solid rgba(214, 164, 90, 0.2);
            border-radius: 16px;
            padding: 2rem;
            margin-bottom: 2rem;
        }

        .guidelines h4 {
            color: var(--gold);
            margin-bottom: 1rem;
        }

        .guidelines ul {
            color: var(--muted);
            margin: 0;
        }

        .guidelines li {
            margin-bottom: 0.5rem;
        }

        @media (max-width: 768px) {
            .art-request-header h1 {
                font-size: 2rem;
            }
            
            .art-form-card {
                padding: 2rem 1.5rem;
            }
            
            .file-upload-area {
                padding: 2rem 1rem;
            }
        }
    </style>
</head>
<body>
    <div class="art-request-container">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <a href="/" class="btn-back">
                        <i class="fas fa-arrow-left me-2"></i>Back to Home
                    </a>
                    
                    <div class="art-request-header">
                        <h1>Submit Your Artwork</h1>
                        <p>Share your creativity with the Rabuste Coffee community. Submit your artwork for a chance to be featured in our gallery.</p>
                    </div>

                    <div class="art-form-card">
                        <!-- Submission Guidelines -->
                        <div class="guidelines">
                            <h4><i class="fas fa-info-circle me-2"></i>Submission Guidelines</h4>
                            <ul>
                                <li>Artwork should be coffee, café, or art-themed</li>
                                <li>Accepted formats: JPG, PNG, PDF (max 10MB)</li>
                                <li>Original work only - no copyrighted material</li>
                                <li>High resolution images preferred (minimum 1200px width)</li>
                                <li>All submissions will be reviewed within 3-5 business days</li>
                            </ul>
                        </div>

                        <form id="artRequestForm" method="POST" action="/submit-art-request" enctype="multipart/form-data">
                            <!-- Artist Information -->
                            <div class="form-section">
                                <h3><i class="fas fa-user me-2"></i>Artist Information</h3>
                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label">Full Name *</label>
                                        <input type="text" class="form-control" name="artistName" required placeholder="Enter your full name">
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label">Email Address *</label>
                                        <input type="email" class="form-control" name="email" required placeholder="your.email@example.com">
                                    </div>
                                </div>
                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label">Phone Number</label>
                                        <input type="tel" class="form-control" name="phone" placeholder="+1 (555) 123-4567">
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label">Instagram Handle</label>
                                        <input type="text" class="form-control" name="instagram" placeholder="@yourusername">
                                    </div>
                                </div>
                            </div>

                            <!-- Artwork Details -->
                            <div class="form-section">
                                <h3><i class="fas fa-palette me-2"></i>Artwork Details</h3>
                                <div class="row">
                                    <div class="col-md-8 mb-3">
                                        <label class="form-label">Artwork Title *</label>
                                        <input type="text" class="form-control" name="title" required placeholder="Give your artwork a title">
                                    </div>
                                    <div class="col-md-4 mb-3">
                                        <label class="form-label">Art Type *</label>
                                        <select class="form-select" name="type" required>
                                            <option value="">Select Type</option>
                                            <option value="painting">Painting</option>
                                            <option value="photography">Photography</option>
                                            <option value="digital">Digital Art</option>
                                            <option value="sculpture">Sculpture</option>
                                            <option value="mixed-media">Mixed Media</option>
                                            <option value="other">Other</option>
                                        </select>
                                    </div>
                                </div>
                                <div class="mb-3">
                                    <label class="form-label">Description *</label>
                                    <textarea class="form-control" name="description" rows="4" required placeholder="Describe your artwork, inspiration, and techniques used..."></textarea>
                                </div>
                                <div class="row">
                                    <div class="col-md-4 mb-3">
                                        <label class="form-label">Dimensions</label>
                                        <input type="text" class="form-control" name="dimensions" placeholder="e.g., 24x36 inches">
                                    </div>
                                    <div class="col-md-4 mb-3">
                                        <label class="form-label">Medium</label>
                                        <input type="text" class="form-control" name="medium" placeholder="e.g., Oil on canvas">
                                    </div>
                                    <div class="col-md-4 mb-3">
                                        <label class="form-label">Year Created</label>
                                        <input type="number" class="form-control" name="year" min="1900" max="2024" placeholder="2024">
                                    </div>
                                </div>
                            </div>

                            <!-- File Upload -->
                            <div class="form-section">
                                <h3><i class="fas fa-cloud-upload-alt me-2"></i>Upload Artwork</h3>
                                <div class="file-upload-area" id="fileUploadArea">
                                    <div class="file-upload-icon">
                                        <i class="fas fa-cloud-upload-alt"></i>
                                    </div>
                                    <div class="file-upload-text">
                                        Drag and drop your artwork here, or click to browse
                                    </div>
                                    <div class="file-upload-subtext">
                                        Supported formats: JPG, PNG, PDF (Max 10MB)
                                    </div>
                                    <input type="file" id="artworkFile" name="artwork" accept=".jpg,.jpeg,.png,.pdf" style="display: none;" required>
                                </div>
                                <div id="filePreview" class="file-preview" style="display: none;"></div>
                            </div>

                            <!-- Additional Information -->
                            <div class="form-section">
                                <h3><i class="fas fa-comment me-2"></i>Additional Information</h3>
                                <div class="mb-3">
                                    <label class="form-label">Artist Statement (Optional)</label>
                                    <textarea class="form-control" name="artistStatement" rows="3" placeholder="Tell us about your artistic journey and what drives your creativity..."></textarea>
                                </div>
                                <div class="mb-3">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" name="forSale" id="forSale">
                                        <label class="form-check-label" for="forSale">
                                            This artwork is available for purchase
                                        </label>
                                    </div>
                                </div>
                                <div class="mb-3" id="priceSection" style="display: none;">
                                    <label class="form-label">Price (USD)</label>
                                    <input type="number" class="form-control" name="price" min="0" step="0.01" placeholder="0.00">
                                </div>
                            </div>

                            <!-- Terms and Submit -->
                            <div class="form-section">
                                <div class="mb-4">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" name="terms" id="terms" required>
                                        <label class="form-check-label" for="terms">
                                            I confirm that this is my original work and I grant Rabuste Coffee permission to display it in their gallery and promotional materials with proper attribution.
                                        </label>
                                    </div>
                                </div>
                                <button type="submit" class="btn-submit">
                                    <i class="fas fa-paper-plane me-2"></i>Submit Artwork
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // File upload functionality
        const fileUploadArea = document.getElementById('fileUploadArea');
        const fileInput = document.getElementById('artworkFile');
        const filePreview = document.getElementById('filePreview');
        const forSaleCheckbox = document.getElementById('forSale');
        const priceSection = document.getElementById('priceSection');

        // File upload area click
        fileUploadArea.addEventListener('click', () => {
            fileInput.click();
        });

        // Drag and drop functionality
        fileUploadArea.addEventListener('dragover', (e) => {
            e.preventDefault();
            fileUploadArea.classList.add('dragover');
        });

        fileUploadArea.addEventListener('dragleave', () => {
            fileUploadArea.classList.remove('dragover');
        });

        fileUploadArea.addEventListener('drop', (e) => {
            e.preventDefault();
            fileUploadArea.classList.remove('dragover');
            const files = e.dataTransfer.files;
            if (files.length > 0) {
                fileInput.files = files;
                handleFileSelect(files[0]);
            }
        });

        // File input change
        fileInput.addEventListener('change', (e) => {
            if (e.target.files.length > 0) {
                handleFileSelect(e.target.files[0]);
            }
        });

        // Handle file selection
        function handleFileSelect(file) {
            const maxSize = 10 * 1024 * 1024; // 10MB
            const allowedTypes = ['image/jpeg', 'image/jpg', 'image/png', 'application/pdf'];

            if (file.size > maxSize) {
                alert('File size must be less than 10MB');
                return;
            }

            if (!allowedTypes.includes(file.type)) {
                alert('Please select a JPG, PNG, or PDF file');
                return;
            }

            // Show file preview
            filePreview.style.display = 'block';
            
            if (file.type.startsWith('image/')) {
                const reader = new FileReader();
                reader.onload = (e) => {
                    filePreview.innerHTML = `
                        <div class="d-flex align-items-center">
                            <img src="${e.target.result}" alt="Preview" class="me-3">
                            <div>
                                <strong>${file.name}</strong><br>
                                <small class="text-muted">${(file.size / 1024 / 1024).toFixed(2)} MB</small>
                            </div>
                        </div>
                    `;
                };
                reader.readAsDataURL(file);
            } else {
                filePreview.innerHTML = `
                    <div class="d-flex align-items-center">
                        <i class="fas fa-file-pdf fa-3x text-danger me-3"></i>
                        <div>
                            <strong>${file.name}</strong><br>
                            <small class="text-muted">${(file.size / 1024 / 1024).toFixed(2)} MB</small>
                        </div>
                    </div>
                `;
            }
        }

        // Show/hide price section
        forSaleCheckbox.addEventListener('change', () => {
            priceSection.style.display = forSaleCheckbox.checked ? 'block' : 'none';
        });

        // Form submission
        document.getElementById('artRequestForm').addEventListener('submit', (e) => {
            e.preventDefault();
            
            // Show loading state
            const submitBtn = document.querySelector('.btn-submit');
            const originalText = submitBtn.innerHTML;
            submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i>Submitting...';
            submitBtn.disabled = true;

            // Simulate form submission (replace with actual submission)
            setTimeout(() => {
                alert('Thank you! Your artwork has been submitted successfully. We will review it within 3-5 business days.');
                submitBtn.innerHTML = originalText;
                submitBtn.disabled = false;
                // Reset form or redirect
                // window.location.href = '/';
            }, 2000);
        });
    </script>
</body>
</html>