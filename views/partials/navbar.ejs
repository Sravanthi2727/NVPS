<nav class="navbar navbar-expand-lg fixed-top navbar-dark px-3 py-3">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">RABUSTE</a>

    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#rabusteNav"
      aria-controls="rabusteNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <i class="bi bi-grid-3x3-gap" style="color: #d6a45a; font-size: 1.2rem;"></i>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="rabusteNav">
      <!-- Close button for mobile - Show on ALL pages -->
      <button class="mobile-close-btn d-lg-none" onclick="closeNavbarMenu()">
        ✕
      </button>
      
      <!-- Header for mobile - Show on ALL pages -->
      <div class="mobile-header d-lg-none">
        <h2>RABUSTE</h2>
      </div>
      
      <div class="navbar-nav align-items-center">
        <!-- Row 1: Home, Gallery, Menu -->
        <div class="nav-row">
          <div class="nav-item">
            <a class="nav-link" href="/"><i class="fas fa-home"></i>Home</a>
          </div>
          <div class="nav-item">
            <a class="nav-link" href="/gallery"><i class="fas fa-palette"></i>Gallery</a>
          </div>
          <div class="nav-item">
            <a class="nav-link" href="/menu"><i class="fas fa-coffee"></i>Menu</a>
          </div>
        </div>
        
        <!-- Row 2: About Us, Workshops, Philosophy -->
        <div class="nav-row">
          <div class="nav-item">
            <a class="nav-link" href="/about"><i class="fas fa-info-circle"></i>About</a>
          </div>
          <div class="nav-item">
            <a class="nav-link" href="/workshops"><i class="fas fa-paint-brush"></i>Workshops</a>
          </div>
          <div class="nav-item">
            <a class="nav-link" href="/philosophy"><i class="fas fa-lightbulb"></i>Philosophy</a>
          </div>
        </div>
        
        <% if (typeof currentUser !== 'undefined' && currentUser) { %>
          <!-- Row 3: Dashboard, Sign Out, Franchise -->
          <div class="nav-row">
            <div class="nav-item d-lg-none">
              <a class="nav-link" href="/user-dashboard">
                <i class="fas fa-tachometer-alt"></i>Dashboard
              </a>
            </div>
            <div class="nav-item d-lg-none">
              <a class="nav-link logout-link" href="/logout">
                <i class="fas fa-sign-out-alt"></i>Sign Out
              </a>
            </div>
            <div class="nav-item">
              <a class="nav-link franchise-btn" href="/franchise">
                <i class="fas fa-handshake"></i>Franchise
              </a>
            </div>
          </div>
          
          <!-- Row 4: User Name -->
          <div class="nav-row">
            <div class="nav-item dropdown d-none d-lg-block">
              <span class="nav-link user-dropdown dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false" style="cursor: pointer;">
                <i class="fas fa-user-circle"></i><%= currentUser.name %>
              </span>
              <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                <li><a class="dropdown-item" href="/user-dashboard">
                  <i class="fas fa-tachometer-alt me-2"></i>My Dashboard
                </a></li>
                <% if (currentUser.role === 'admin') { %>
                  <li><hr class="dropdown-divider"></li>
                  <li><a class="dropdown-item admin-link" href="/admin">
                    <i class="fas fa-cog me-2"></i>Admin Panel
                  </a></li>
                <% } %>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item logout-link" href="/logout">
                  <i class="fas fa-sign-out-alt me-2"></i>Sign Out
                </a></li>
              </ul>
            </div>
            <div class="nav-item d-lg-none">
              <span class="nav-link user-dropdown">
                <i class="fas fa-user-circle"></i><%= currentUser.name %>
              </span>
            </div>
            <div class="nav-item empty">
              <!-- Empty space -->
            </div>
          </div>
          
          <% if (currentUser.role === 'admin') { %>
            <div class="nav-item d-lg-none" style="display: none;">
              <a class="nav-link admin-link" href="/admin">
                <i class="fas fa-cog"></i>Admin Panel
              </a>
            </div>
          <% } %>
        <% } else { %>
          <!-- Row 3: Sign In, Franchise (when not logged in) -->
          <div class="nav-row">
            <div class="nav-item">
              <a class="nav-link signin-btn" href="/signin">
                <i class="fas fa-sign-in-alt"></i>Sign In
              </a>
            </div>
            <div class="nav-item">
              <a class="nav-link franchise-btn" href="/franchise">
                <i class="fas fa-handshake"></i>Franchise
              </a>
            </div>
            <div class="nav-item empty">
              <!-- Empty space -->
            </div>
          </div>
        <% } %>
      </div>
    </div>
  </div>
</nav>

<style>
/* Mobile Navbar Improvements */
@media (max-width: 991.98px) {
  .navbar-brand {
    font-size: 1.3rem !important;
  }
  
  .navbar-collapse {
    background-color: rgba(0, 0, 0, 0.95) !important;
    border-radius: 10px !important;
    margin-top: 1rem !important;
    padding: 1rem !important;
    border: 1px solid #d6a45a !important;
  }
  
  .navbar-nav {
    gap: 0.5rem !important;
  }
  
  .nav-link {
    padding: 0.7rem 1rem !important;
    border-radius: 8px !important;
    transition: all 0.3s ease !important;
  }
  
  .nav-link:hover {
    background-color: rgba(214, 164, 90, 0.1) !important;
    color: #d6a45a !important;
  }
  
  .dropdown-menu {
    background-color: rgba(0, 0, 0, 0.95) !important;
    border: 1px solid #d6a45a !important;
    border-radius: 8px !important;
  }
  
  .dropdown-item {
    color: #f5f2ee !important;
    padding: 0.5rem 1rem !important;
  }
  
  .dropdown-item:hover {
    background-color: rgba(214, 164, 90, 0.1) !important;
    color: #d6a45a !important;
  }
  
  .btn-outline-light {
    margin-top: 0.5rem !important;
    width: 100% !important;
    text-align: center !important;
  }
}

@media (max-width: 576px) {
  .navbar-brand {
    font-size: 1.1rem !important;
    letter-spacing: 1px !important;
  }
  
  .navbar-toggler {
    padding: 0.3rem 0.5rem !important;
    font-size: 1rem !important;
  }
  
  .nav-link {
    font-size: 0.9rem !important;
    padding: 0.6rem 0.8rem !important;
  }
  
  .dropdown-item {
    font-size: 0.9rem !important;
  }
}
</style>

<script>
// Function to close navbar menu
function closeNavbarMenu() {
    const navbarCollapse = document.getElementById('rabusteNav');
    const navbarToggler = document.querySelector('.navbar-toggler');
    
    if (navbarCollapse && navbarCollapse.classList.contains('show')) {
        // Use Bootstrap's collapse method to close
        const bsCollapse = new bootstrap.Collapse(navbarCollapse, {
            toggle: false
        });
        bsCollapse.hide();
        
        // Update toggler state
        if (navbarToggler) {
            navbarToggler.setAttribute('aria-expanded', 'false');
            navbarToggler.classList.add('collapsed');
            
            // Reset icon to grid
            const icon = navbarToggler.querySelector('i');
            if (icon) {
                icon.className = 'bi bi-grid-3x3-gap';
            }
        }
    }
}

document.addEventListener('DOMContentLoaded', function() {
    // Handle navbar toggle icon change
    const navbarToggler = document.querySelector('.navbar-toggler');
    const navbarCollapse = document.getElementById('rabusteNav');
    
    if (navbarToggler && navbarCollapse) {
        navbarCollapse.addEventListener('show.bs.collapse', function() {
            const icon = navbarToggler.querySelector('i');
            if (icon) {
                icon.className = 'bi bi-x-lg';
            }
        });
        
        navbarCollapse.addEventListener('hide.bs.collapse', function() {
            const icon = navbarToggler.querySelector('i');
            if (icon) {
                icon.className = 'bi bi-grid-3x3-gap';
            }
        });
    }
    
    // Close menu when clicking on a nav link
    document.querySelectorAll('.navbar-nav .nav-link').forEach(link => {
        link.addEventListener('click', function(e) {
            // Don't close if it's a dropdown toggle
            if (!this.classList.contains('dropdown-toggle')) {
                setTimeout(() => {
                    closeNavbarMenu();
                }, 100);
            }
        });
    });
    
    // Close menu when clicking outside
    document.addEventListener('click', function(e) {
        const navbarCollapse = document.getElementById('rabusteNav');
        const navbarToggler = document.querySelector('.navbar-toggler');
        
        if (navbarCollapse && navbarCollapse.classList.contains('show')) {
            // Check if click is outside the navbar
            if (!navbarCollapse.contains(e.target) && !navbarToggler.contains(e.target)) {
                closeNavbarMenu();
            }
        }
    });
});
</script>