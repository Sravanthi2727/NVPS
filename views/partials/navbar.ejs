<nav class="navbar navbar-expand-lg fixed-top navbar-dark px-3 py-3">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">RABUSTE</a>

    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#rabusteNav"
      aria-controls="rabusteNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="rabusteNav">
      <!-- Close button for mobile - Show on ALL pages -->
      <button class="mobile-close-btn d-lg-none" onclick="closeNavbarMenu()">
        ✕
      </button>
      
      <!-- Header for mobile - Show on ALL pages -->
      <div class="mobile-header d-lg-none">
        <h2>RABUSTE</h2>
      </div>
      
      <ul class="navbar-nav align-items-center">
        <li class="nav-item">
          <a class="nav-link" href="/"><i class="fas fa-home"></i>Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/philosophy"><i class="fas fa-lightbulb"></i>Philosophy</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/menu"><i class="fas fa-coffee"></i>Menu</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/gallery"><i class="fas fa-palette"></i>Art Gallery</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/workshops"><i class="fas fa-paint-brush"></i>Workshops</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/about"><i class="fas fa-info-circle"></i>About Us</a>
        </li>
        <% if (typeof currentUser !== 'undefined' && currentUser) { %>
          <!-- Desktop: Dropdown, Mobile: Inline items -->
          <li class="nav-item dropdown d-none d-lg-block">
            <a class="nav-link dropdown-toggle user-dropdown" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="fas fa-user-circle me-1"></i><%= currentUser.name %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <li><a class="dropdown-item" href="/user-dashboard">
                <i class="fas fa-tachometer-alt me-2"></i>My Dashboard
              </a></li>
              <% if (currentUser.role === 'admin') { %>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item admin-link" href="/admin">
                  <i class="fas fa-cog me-2"></i>Admin Panel
                </a></li>
              <% } %>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item logout-link" href="/logout">
                <i class="fas fa-sign-out-alt me-2"></i>Sign Out
              </a></li>
            </ul>
          </li>
          
          <!-- Mobile: Inline items -->
          <li class="nav-item d-lg-none">
            <span class="nav-link user-info">
              <i class="fas fa-user-circle me-1"></i><%= currentUser.name %>
            </span>
          </li>
          <li class="nav-item d-lg-none">
            <a class="nav-link" href="/user-dashboard">
              <i class="fas fa-tachometer-alt"></i>My Dashboard
            </a>
          </li>
          <% if (currentUser.role === 'admin') { %>
            <li class="nav-item d-lg-none">
              <a class="nav-link admin-link" href="/admin">
                <i class="fas fa-cog"></i>Admin Panel
              </a>
            </li>
          <% } %>
          <li class="nav-item d-lg-none">
            <a class="nav-link logout-link" href="/logout">
              <i class="fas fa-sign-out-alt"></i>Sign Out
            </a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link signin-btn" href="/signin">
              <i class="fas fa-sign-in-alt me-1"></i>Sign In
            </a>
          </li>
        <% } %>
        <li class="nav-item">
          <a class="nav-link btn btn-outline-light px-3 rounded-pill franchise-btn" href="/franchise">
            <i class="fas fa-handshake"></i>Franchise
          </a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<style>
/* Mobile Navbar Improvements */
@media (max-width: 991.98px) {
  .navbar-brand {
    font-size: 1.3rem !important;
  }
  
  .navbar-collapse {
    background-color: rgba(0, 0, 0, 0.95) !important;
    border-radius: 10px !important;
    margin-top: 1rem !important;
    padding: 1rem !important;
    border: 1px solid #d6a45a !important;
  }
  
  .navbar-nav {
    gap: 0.5rem !important;
  }
  
  .nav-link {
    padding: 0.7rem 1rem !important;
    border-radius: 8px !important;
    transition: all 0.3s ease !important;
  }
  
  .nav-link:hover {
    background-color: rgba(214, 164, 90, 0.1) !important;
    color: #d6a45a !important;
  }
  
  .dropdown-menu {
    background-color: rgba(0, 0, 0, 0.95) !important;
    border: 1px solid #d6a45a !important;
    border-radius: 8px !important;
  }
  
  .dropdown-item {
    color: #f5f2ee !important;
    padding: 0.5rem 1rem !important;
  }
  
  .dropdown-item:hover {
    background-color: rgba(214, 164, 90, 0.1) !important;
    color: #d6a45a !important;
  }
  
  .btn-outline-light {
    margin-top: 0.5rem !important;
    width: 100% !important;
    text-align: center !important;
  }
}

@media (max-width: 576px) {
  .navbar-brand {
    font-size: 1.1rem !important;
    letter-spacing: 1px !important;
  }
  
  .navbar-toggler {
    padding: 0.3rem 0.5rem !important;
    font-size: 1rem !important;
  }
  
  .nav-link {
    font-size: 0.9rem !important;
    padding: 0.6rem 0.8rem !important;
  }
  
  .dropdown-item {
    font-size: 0.9rem !important;
  }
}
</style>

<script>
// Function to close navbar menu
function closeNavbarMenu() {
    const navbarCollapse = document.getElementById('rabusteNav');
    const navbarToggler = document.querySelector('.navbar-toggler');
    
    if (navbarCollapse && navbarCollapse.classList.contains('show')) {
        // Use Bootstrap's collapse method to close
        const bsCollapse = new bootstrap.Collapse(navbarCollapse, {
            toggle: false
        });
        bsCollapse.hide();
        
        // Update toggler state
        if (navbarToggler) {
            navbarToggler.setAttribute('aria-expanded', 'false');
            navbarToggler.classList.add('collapsed');
        }
    }
}

// Force grid layout on mobile - Flexible grid
function forceGridLayout() {
    const navbarNav = document.querySelector('.navbar-nav');
    if (navbarNav && window.innerWidth <= 991) {
        navbarNav.style.display = 'grid';
        navbarNav.style.gridTemplateColumns = 'repeat(auto-fit, minmax(150px, 1fr))';
        navbarNav.style.gridGap = '1rem';
        navbarNav.style.width = '100%';
        navbarNav.style.marginTop = '2rem';
        navbarNav.style.marginBottom = '2rem';
        
        // Force each nav item to be a grid item
        const navItems = navbarNav.querySelectorAll('.nav-item');
        navItems.forEach(item => {
            item.style.margin = '0';
            item.style.display = 'block';
        });
    }
}

document.addEventListener('DOMContentLoaded', function() {
    // Force grid layout on load
    forceGridLayout();
    
    // Force grid layout when navbar is shown
    const navbarCollapse = document.getElementById('rabusteNav');
    if (navbarCollapse) {
        navbarCollapse.addEventListener('shown.bs.collapse', function() {
            setTimeout(forceGridLayout, 100);
        });
    }
    
    // Force grid layout on window resize
    window.addEventListener('resize', forceGridLayout);
    
    // Close menu when clicking on a nav link
    document.querySelectorAll('.navbar-nav .nav-link').forEach(link => {
        link.addEventListener('click', function(e) {
            // Don't close if it's a dropdown toggle
            if (!this.classList.contains('dropdown-toggle')) {
                setTimeout(() => {
                    closeNavbarMenu();
                }, 100);
            }
        });
    });
    
    // Close menu when clicking outside
    document.addEventListener('click', function(e) {
        const navbarCollapse = document.getElementById('rabusteNav');
        const navbarToggler = document.querySelector('.navbar-toggler');
        
        if (navbarCollapse && navbarCollapse.classList.contains('show')) {
            // Check if click is outside the navbar
            if (!navbarCollapse.contains(e.target) && !navbarToggler.contains(e.target)) {
                closeNavbarMenu();
            }
        }
    });
});
</script>