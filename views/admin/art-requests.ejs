<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Art Requests - Admin Dashboard</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        /* Complete CSS Reset for Admin */
        * {
            margin: 0 !important;
            padding: 0 !important;
            box-sizing: border-box !important;
        }
        
        html, body {
            margin: 0 !important;
            padding: 0 !important;
            font-family: 'Inter', sans-serif !important;
            background: #000 !important;
            color: #fff !important;
            overflow-x: hidden !important;
            height: 100% !important;
        }
        
        /* Admin Layout */
        .admin-wrapper {
            display: flex !important;
            min-height: 100vh !important;
            background: #000 !important;
        }
        
        /* Sidebar */
        .admin-sidebar {
            width: 250px !important;
            background: #111 !important;
            border-right: 2px solid #d6a45a !important;
            position: fixed !important;
            height: 100vh !important;
            overflow-y: auto !important;
            z-index: 9999 !important;
        }
        
        .admin-logo {
            padding: 2rem 1.5rem !important;
            text-align: center !important;
            border-bottom: 1px solid #333 !important;
        }
        
        .admin-logo h4 {
            color: #d6a45a !important;
            margin: 0 !important;
            font-weight: 700 !important;
        }
        
        .admin-nav {
            padding: 1rem 0 !important;
        }
        
        .admin-nav a {
            display: block !important;
            padding: 1rem 1.5rem !important;
            color: #fff !important;
            text-decoration: none !important;
            transition: all 0.3s ease !important;
        }
        
        .admin-nav a:hover {
            background: rgba(214, 164, 90, 0.1) !important;
            color: #d6a45a !important;
        }
        
        .admin-nav a.active {
            background: #d6a45a !important;
            color: #000 !important;
        }
        
        .admin-nav i {
            width: 20px !important;
            margin-right: 10px !important;
        }
        
        /* Main Content */
        .admin-main {
            flex: 1 !important;
            margin-left: 250px !important;
            background: #000 !important;
        }
        
        .admin-header {
            background: #1a1a1a !important;
            padding: 1rem 2rem !important;
            border-bottom: 1px solid #333 !important;
            position: sticky !important;
            top: 0 !important;
            z-index: 100 !important;
        }
        
        .admin-header h2 {
            color: #d6a45a !important;
            margin: 0 !important;
        }
        
        .admin-content {
            padding: 2rem !important;
        }
        
        /* Cards */
        .admin-card {
            background: #1a1a1a !important;
            border: 1px solid #333 !important;
            border-radius: 8px !important;
            padding: 1.5rem !important;
            margin-bottom: 1rem !important;
            transition: transform 0.3s ease !important;
        }
        
        .admin-card:hover {
            transform: translateY(-2px) !important;
            border-color: #d6a45a !important;
        }
        
        .admin-card h3 {
            color: #d6a45a !important;
            margin-bottom: 1rem !important;
        }
        
        /* Tables */
        .admin-table {
            background: #1a1a1a !important;
            border: 1px solid #333 !important;
            border-radius: 8px !important;
            overflow: hidden !important;
        }
        
        .admin-table table {
            width: 100% !important;
            margin: 0 !important;
        }
        
        .admin-table th {
            background: #333 !important;
            color: #d6a45a !important;
            padding: 1rem !important;
            border: none !important;
        }
        
        .admin-table td {
            padding: 1rem !important;
            border-bottom: 1px solid #333 !important;
            color: #fff !important;
        }
        
        .admin-table tr:hover {
            background: rgba(214, 164, 90, 0.1) !important;
        }
        
        /* Buttons */
        .btn-primary {
            background: #d6a45a !important;
            border-color: #d6a45a !important;
            color: #000 !important;
        }
        
        .btn-primary:hover {
            background: #e3b873 !important;
            border-color: #e3b873 !important;
            color: #000 !important;
        }
        
        .btn-outline-primary {
            color: #d6a45a !important;
            border-color: #d6a45a !important;
        }
        
        .btn-outline-primary:hover {
            background: #d6a45a !important;
            color: #000 !important;
        }
        
        /* Form Controls */
        .form-control, .form-select {
            background: #333 !important;
            border: 1px solid #555 !important;
            color: #fff !important;
        }
        
        .form-control:focus, .form-select:focus {
            background: #333 !important;
            border-color: #d6a45a !important;
            color: #fff !important;
            box-shadow: 0 0 0 0.2rem rgba(214, 164, 90, 0.25) !important;
        }
        
        .form-label {
            color: #d6a45a !important;
        }
        
        /* Badges */
        .badge {
            padding: 0.5rem 0.75rem !important;
        }
        
        .badge.bg-warning {
            background: #f39c12 !important;
            color: #000 !important;
        }
        
        .badge.bg-success {
            background: #27ae60 !important;
        }
        
        .badge.bg-info {
            background: #d6a45a !important;
            color: #000 !important;
        }
        
        .badge.bg-primary {
            background: #d6a45a !important;
            color: #000 !important;
        }
        
        .badge.bg-danger {
            background: #e74c3c !important;
            color: #fff !important;
        }
        
        /* Mobile */
        @media (max-width: 768px) {
            .admin-sidebar {
                transform: translateX(-100%) !important;
                z-index: 10000 !important;
            }
            
            .admin-sidebar.show {
                transform: translateX(0) !important;
            }
            
            .admin-main {
                margin-left: 0 !important;
            }
        }
        
        .mobile-toggle {
            display: none !important;
            background: #d6a45a !important;
            border: none !important;
            color: #000 !important;
            padding: 0.5rem !important;
            border-radius: 4px !important;
        }
        
        @media (max-width: 768px) {
            .mobile-toggle {
                display: inline-block !important;
            }
        }
    </style>
</head>
<body>
    <div class="admin-wrapper">
        <!-- Sidebar -->
        <nav class="admin-sidebar" id="adminSidebar">
            <div class="admin-logo">
                <h4><i class="fas fa-coffee me-2"></i>Rabuste Admin</h4>
            </div>
            
            <div class="admin-nav">
                <a href="/admin">
                    <i class="fas fa-tachometer-alt"></i>Dashboard
                </a>
                <a href="/admin/cart-requests">
                    <i class="fas fa-shopping-cart"></i>Cart Requests
                </a>
                <a href="/admin/art-requests" class="active">
                    <i class="fas fa-palette"></i>Art Requests
                </a>
                <a href="/admin/workshop-requests">
                    <i class="fas fa-calendar-alt"></i>Workshop Requests
                </a>
                <a href="/admin/franchise">
                    <i class="fas fa-handshake"></i>Franchise Applications
                </a>
                
                <hr style="border-color: #333; margin: 1rem 1.5rem;">
                
                <a href="/" target="_blank">
                    <i class="fas fa-external-link-alt"></i>View Website
                </a>
                <a href="/logout">
                    <i class="fas fa-sign-out-alt"></i>Logout
                </a>
            </div>
        </nav>
        
        <!-- Main Content -->
        <main class="admin-main">
            <div class="admin-header d-flex justify-content-between align-items-center">
                <div class="d-flex align-items-center">
                    <button class="mobile-toggle me-3" onclick="toggleSidebar()">
                        <i class="fas fa-bars"></i>
                    </button>
                    <h2>Art Requests</h2>
                </div>
                <div>
                    <span style="color: #ccc;">Welcome, Admin</span>
                </div>
            </div>
            
            <div class="admin-content">
                <!-- Art Requests Content -->
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <div>
                        <p style="color: #ccc;">Manage art purchase orders from customers</p>
                    </div>
                </div>

                <!-- Filters -->
                <div class="admin-card mb-4">
                    <div class="row g-3">
                        <div class="col-md-4">
                            <label class="form-label" style="color: #d6a45a;">Status</label>
                            <select class="form-select bg-dark text-light border-secondary" id="statusFilter">
                                <option value="">All Status</option>
                                <option value="pending">Pending</option>
                                <option value="completed">Completed</option>
                                <option value="cancelled">Cancelled</option>
                            </select>
                        </div>
                        <div class="col-md-4">
                            <label class="form-label" style="color: #d6a45a;">Payment Method</label>
                            <select class="form-select bg-dark text-light border-secondary" id="paymentFilter">
                                <option value="">All Payment Methods</option>
                                <option value="online">Online</option>
                                <option value="cod">Cash on Delivery</option>
                                <option value="cash">Cash</option>
                            </select>
                        </div>
                        <div class="col-md-4">
                            <label class="form-label" style="color: #d6a45a;">Search</label>
                            <input type="text" class="form-control bg-dark text-light border-secondary" placeholder="Search by customer name or email..." id="searchInput">
                        </div>
                    </div>
                </div>

     <!-- Art Requests Table -->
     <div class="admin-card">
        <h3 class="mb-3">Art Purchase Orders</h3>
        <div class="admin-table">
            <table class="table table-dark">
                <thead>
                    <tr>
                        <th>Customer</th>
                        <th>Art Items</th>
                        <th>Address</th>
                        <th>Payment</th>
                        <th>Total</th>
                        <th>Order Date</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (artRequests && artRequests.length > 0) { %>
                        <% artRequests.forEach(order => { %>
                        <tr>
                            <td>
                                <strong><%= order.customerName %></strong><br>
                                <small style="color: #ccc;"><%= order.customerEmail %></small>
                            </td>
                            <td>
                                <% if (order.items && order.items.length > 0) { %>
                                    <% order.items.forEach((item, index) => { %>
                                        <div class="mb-2">
                                            <div class="d-flex align-items-center">
                                                <% if (item.image) { %>
                                                    <img src="<%= item.image %>" alt="<%= item.name %>" style="width: 40px; height: 40px; object-fit: cover; border-radius: 4px; margin-right: 8px;">
                                                <% } else { %>
                                                    <div style="width: 40px; height: 40px; background: #d6a45a; border-radius: 4px; display: flex; align-items: center; justify-content: center; margin-right: 8px;">
                                                        <i class="fas fa-palette" style="color: #000; font-size: 14px;"></i>
                                                    </div>
                                                <% } %>
                                                <div>
                                                    <strong style="font-size: 0.9rem;"><%= item.name %></strong><br>
                                                    <small style="color: #ccc;">Qty: <%= item.quantity %> × ₹<%= item.price %></small>
                                                </div>
                                            </div>
                                        </div>
                                    <% }); %>
                                <% } else { %>
                                    <span style="color: #ccc;">No items</span>
                                <% } %>
                            </td>
                            <td>
                                <% if (order.deliveryAddress) { %>
                                    <small style="color: #ccc; line-height: 1.6;">
                                        <%= order.deliveryAddress.name %><br>
                                        <%= order.deliveryAddress.phone %><br>
                                        <%= order.deliveryAddress.address %><br>
                                        <%= order.deliveryAddress.city %><% if (order.deliveryAddress.state) { %>, <%= order.deliveryAddress.state %><% } %><br>
                                        <%= order.deliveryAddress.pincode %>
                                    </small>
                                <% } else { %>
                                    <span style="color: #999;">No address</span>
                                <% } %>
                            </td>
                            <td>
                                <% if (order.paymentMethod === 'online') { %>
                                    <span class="badge bg-success">Online</span>
                                <% } else if (order.paymentMethod === 'cod') { %>
                                    <span class="badge bg-warning">COD</span>
                                <% } else if (order.paymentMethod === 'cash') { %>
                                    <span class="badge bg-info">Cash</span>
                                <% } else { %>
                                    <span class="badge bg-secondary"><%= order.paymentMethod || 'N/A' %></span>
                                <% } %>
                            </td>
                            <td>
                                <strong style="color: #d6a45a;">₹<%= order.totalAmount %></strong>
                            </td>
                            <td>
                                <%= new Date(order.orderDate || order.createdAt).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %>
                            </td>
                            <td>
                                <% if (order.status === 'pending') { %>
                                    <span class="badge bg-warning">Pending</span>
                                <% } else if (order.status === 'completed') { %>
                                    <span class="badge bg-success">Completed</span>
                                <% } else if (order.status === 'cancelled') { %>
                                    <span class="badge bg-danger">Cancelled</span>
                                <% } else { %>
                                    <span class="badge bg-secondary"><%= order.status %></span>
                                <% } %>
                            </td>
                            <td>
                                <div class="btn-group" role="group">
                                    <button class="btn btn-sm btn-outline-primary" 
                                        data-order-id="<%= order._id %>"
                                        data-customer-name="<%= (order.customerName || '').replace(/"/g, '&quot;') %>"
                                        data-customer-email="<%= (order.customerEmail || '').replace(/"/g, '&quot;') %>"
                                        data-address="<%= order.deliveryAddress ? JSON.stringify(order.deliveryAddress).replace(/"/g, '&quot;') : '' %>"
                                        data-payment="<%= order.paymentMethod || '' %>"
                                        data-total="<%= order.totalAmount || 0 %>"
                                        data-items="<%= JSON.stringify(order.items || []).replace(/"/g, '&quot;') %>"
                                        onclick="viewOrderDetailsFromButton(this)">
                                        <i class="fas fa-eye"></i>
                                    </button>
                                    <% if (order.status === 'pending') { %>
                                    <form method="POST" action="/admin/art-requests/<%= order._id %>/update" class="d-inline">
                                        <input type="hidden" name="status" value="completed">
                                        <button type="submit" class="btn btn-sm btn-success" title="Mark as Completed">
                                            <i class="fas fa-check"></i>
                                        </button>
                                    </form>
                                    <form method="POST" action="/admin/art-requests/<%= order._id %>/update" class="d-inline">
                                        <input type="hidden" name="status" value="cancelled">
                                        <button type="submit" class="btn btn-sm btn-danger" title="Cancel Order">
                                            <i class="fas fa-times"></i>
                                        </button>
                                    </form>
                                    <% } %>
                                </div>
                            </td>
                        </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="8" class="text-center" style="color: #ccc; padding: 2rem;">
                                <i class="fas fa-palette" style="font-size: 3rem; margin-bottom: 1rem; opacity: 0.3;"></i><br>
                                No art purchase orders found.<br>
                                <small>Art purchase orders will appear here when customers buy art items.</small>
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</div>
</main>
</div>
    
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        function toggleSidebar() {
            document.getElementById('adminSidebar').classList.toggle('show');
        }
        
        // Close sidebar when clicking outside on mobile
        document.addEventListener('click', function(e) {
            const sidebar = document.getElementById('adminSidebar');
            const toggle = document.querySelector('.mobile-toggle');
            
            if (window.innerWidth <= 768 && 
                !sidebar.contains(e.target) && 
                !toggle.contains(e.target) && 
                sidebar.classList.contains('show')) {
                sidebar.classList.remove('show');
            }
        });
        
        function viewOrderDetailsFromButton(button) {
            const orderId = button.getAttribute('data-order-id');
            const customerName = button.getAttribute('data-customer-name');
            const customerEmail = button.getAttribute('data-customer-email');
            const paymentMethod = button.getAttribute('data-payment');
            const totalAmount = parseFloat(button.getAttribute('data-total')) || 0;
            
            let deliveryAddress = null;
            const addressStr = button.getAttribute('data-address');
            if (addressStr) {
                try {
                    deliveryAddress = JSON.parse(addressStr.replace(/&quot;/g, '"'));
                } catch (e) {
                    console.error('Error parsing address:', e);
                }
            }
            
            let items = [];
            const itemsStr = button.getAttribute('data-items');
            if (itemsStr) {
                try {
                    items = JSON.parse(itemsStr.replace(/&quot;/g, '"'));
                } catch (e) {
                    console.error('Error parsing items:', e);
                }
            }
            
            viewOrderDetails(orderId, customerName, customerEmail, deliveryAddress, paymentMethod, totalAmount, items);
        }
        
        function viewOrderDetails(orderId, customerName, customerEmail, deliveryAddress, paymentMethod, totalAmount, items) {
            // Parse items if it's a string
            if (typeof items === 'string') {
                items = JSON.parse(items.replace(/&quot;/g, '"'));
            }
            if (typeof deliveryAddress === 'string') {
                deliveryAddress = JSON.parse(deliveryAddress.replace(/&quot;/g, '"'));
            }
            
            // Build items HTML
            let itemsHtml = '';
            if (items && items.length > 0) {
                items.forEach(item => {
                    itemsHtml += `
                        <div class="d-flex align-items-center mb-3 p-2" style="background: #1a1a1a; border-radius: 4px;">
                            ${item.image ? `<img src="${item.image}" alt="${item.name}" style="width: 60px; height: 60px; object-fit: cover; border-radius: 4px; margin-right: 12px;">` : ''}
                            <div style="flex: 1;">
                                <strong>${item.name}</strong><br>
                                <small style="color: #ccc;">Quantity: ${item.quantity} × ₹${item.price} = ₹${item.quantity * item.price}</small>
                            </div>
                        </div>
                    `;
                });
            }
            
            // Build address HTML
            let addressHtml = 'No address provided';
            if (deliveryAddress) {
                addressHtml = `
                    <strong>${deliveryAddress.name || ''}</strong><br>
                    ${deliveryAddress.phone ? `<small>Phone: ${deliveryAddress.phone}</small><br>` : ''}
                    ${deliveryAddress.address || ''}<br>
                    ${deliveryAddress.city || ''}${deliveryAddress.state ? `, ${deliveryAddress.state}` : ''}<br>
                    ${deliveryAddress.pincode || ''}
                `;
            }
            
            // Create and show modal with order details
            const modalHtml = `
                <div class="modal fade" id="orderModal" tabindex="-1">
                    <div class="modal-dialog modal-lg">
                        <div class="modal-content" style="background: #111; color: #fff; border: 1px solid #d6a45a;">
                            <div class="modal-header" style="border-bottom: 1px solid #d6a45a;">
                                <h5 class="modal-title" style="color: #d6a45a;">Art Order Details</h5>
                                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                            </div>
                            <div class="modal-body">
                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <h6 style="color: #d6a45a;">Customer Information</h6>
                                        <p><strong>Name:</strong> ${customerName}</p>
                                        <p><strong>Email:</strong> ${customerEmail}</p>
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <h6 style="color: #d6a45a;">Payment Information</h6>
                                        <p><strong>Payment Method:</strong> ${paymentMethod === 'online' ? 'Online Payment' : paymentMethod === 'cod' ? 'Cash on Delivery' : paymentMethod === 'cash' ? 'Cash' : paymentMethod}</p>
                                        <p><strong>Total Amount:</strong> <span style="color: #d6a45a; font-size: 1.2rem;">₹${totalAmount}</span></p>
                                    </div>
                                </div>
                                <div class="mb-3">
                                    <h6 style="color: #d6a45a;">Delivery Address</h6>
                                    <div style="color: #ccc; line-height: 1.8;">
                                        ${addressHtml}
                                    </div>
                                </div>
                                <div class="mb-3">
                                    <h6 style="color: #d6a45a;">Art Items</h6>
                                    ${itemsHtml || '<p style="color: #ccc;">No items found</p>'}
                                </div>
                            </div>
                            <div class="modal-footer" style="border-top: 1px solid #d6a45a;">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                            </div>
                        </div>
                    </div>
                </div>
            `;
            
            // Remove existing modal if any
            const existingModal = document.getElementById('orderModal');
            if (existingModal) {
                existingModal.remove();
            }
            
            // Add modal to body
            document.body.insertAdjacentHTML('beforeend', modalHtml);
            
            // Show modal
            const modal = new bootstrap.Modal(document.getElementById('orderModal'));
            modal.show();
            
            // Clean up modal after it's hidden
            document.getElementById('orderModal').addEventListener('hidden.bs.modal', function() {
                this.remove();
            });
        }

        function clearFilters() {
            document.getElementById('statusFilter').value = '';
            document.getElementById('paymentFilter').value = '';
            document.getElementById('searchInput').value = '';
        }
    </script>
</body>
</html>